App = Ember.Application.create();

App.Router.map(function() {
  // Define your routes
});

App.IndexRoute = Ember.Route.extend({
  // Customize the behavior of the route
  // Example ->
  // model: function() {
  //  return ['one', 'two', 'three'];
  // }
});
